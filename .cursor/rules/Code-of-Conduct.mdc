---
description: 
alwaysApply: true
---

保持目录与命名空间一一对应（ECS/ 为库、Test/ 为测试，CoreECS.* 与目录同步）；分层职责固定（Defines/ 仅接口与契约，Managers/ 承载有状态管理实现，Utils/ 提供通用基础设施）；对外能力优先收敛到 World、Entity 与 *Extension，不暴露底层存储细节；命名遵循接口 I*、管理器 *Manager、扩展 *Extension、测试 *TestUnit，私有字段默认 m_ 前缀并保持单文件风格一致；注释与命名统一使用英文，且 API 命名用词必须准确、清晰、语义明确，避免歧义或过度缩写；公共操作先进行生命周期与有效性校验，非法状态必须显式抛异常，前置条件优先 Assertion/异常，禁止静默失败；性能敏感路径优先减少分配，优先使用池化、数组、缓存引用与紧凑存储；公共 API 与关键结构需补全 XML 注释（用途、参数、返回、异常）；所有变更必须配套测试（新功能补功能测试，性能或生命周期相关改动补压力/回归测试）；并严格遵循现有 csproj 约定，不随意改动库与测试项目在 Nullable、ImplicitUsings 等配置上的既有差异。
